@model Certificates2024.Models.CertificateTopic


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>

    <div>
        <h4>CertificateTopic</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.TopicName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.TopicName)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Description)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Description)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Price)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Price)
            </dd>
        </dl>
    </div>
    <div>
        @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
        {
            <a asp-action="Edit" class="link-body-emphasis" asp-route-id="@Model?.Id">Edit</a>

        }
        <a asp-action="Index" class="link-body-emphasis">Back to List</a>
    </div>

@* <div>
    <h4>CertificateTopic</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TopicName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TopicName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="link-body-emphasis" asp-route-id="@Model?.Id">Edit</a> |
        <a asp-action="Index" class="link-body-emphasis">Back to List</a>
</div> *@

</body>
</html>
