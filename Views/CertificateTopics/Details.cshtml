@model Certificates2024.Models.CertificateTopic

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div>
        <h4>CertificateTopic</h4>
        <hr />
        <table class="table table-striped table-bordered">
            <tbody>
                <tr>
                    <th> @Html.DisplayNameFor(model => model.TopicName) </th>
                    <td> @Html.DisplayFor(model => model.TopicName) </td>
                </tr>
                <tr>
                    <th> @Html.DisplayNameFor(model => model.Description) </th>
                    <td> @Html.DisplayFor(model => model.Description) </td>
                </tr>
                <tr>
                    <th> @Html.DisplayNameFor(model => model.Price) </th>
                    <td> @Html.DisplayFor(model => model.Price) </td>
                </tr>
            </tbody>
        </table>
    </div>
    <div>
        @if (User.Identity.IsAuthenticated && User.IsInRole("User"))
        {
            <a asp-controller="CandidateCertificates" asp-action="Create" asp-route-topicId="@Model.Id" class="btn btn-secondary btn-sm">Book Now</a>
        }
    </div>
    <br />
    <div>
        @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
        {
            <a asp-action="Edit" class="link-body-emphasis" asp-route-id="@Model?.Id">Edit</a>
        }
        <a asp-action="Index" class="link-body-emphasis">Back to List</a>
    </div>
</body>
</html>


    @* <div>
    <h4>CertificateTopic</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TopicName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TopicName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="link-body-emphasis" asp-route-id="@Model?.Id">Edit</a> |
        <a asp-action="Index" class="link-body-emphasis">Back to List</a>
</div> *@

@* </body>
</html>
 *@